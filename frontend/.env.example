# Environment configuration for frontend

# API Base URL - will be injected at build time
# For local development, use the backend URL
# For production, this should be the ALB URL
VITE_API_BASE_URL=http://localhost:8000

# Superuser name for the application
VITE_SUPERUSER_NAME=admin

# CloudFront domain (will be set during deployment)
# VITE_CLOUDFRONT_DOMAIN=https://d1234567890123.cloudfront.net
